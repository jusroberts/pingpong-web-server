= form_for @room do |f|
  - if @room.errors.any?
    #error_explanation
      %h2= "#{pluralize(@room.errors.count, "error")} prohibited this room from being saved:"
      %ul
        - @room.errors.full_messages.each do |msg|
          %li= msg

  .field
    eventually this will be generated
    = f.label :client_token
    = f.text_field :client_token
  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
